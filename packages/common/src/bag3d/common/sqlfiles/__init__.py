"""SQL files which are loaded for execution by [bag3d.common.utils.database.load_sql]().
The SQL script can contain parameters in the form of ``${...}``, which is
understood by most database managers."""